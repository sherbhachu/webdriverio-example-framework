import { defineSupportCode } from 'cucumber';

defineSupportCode(function ({And, But, Given, Then, When}) {
    Then(/^I can check out the contents of my basket$/, function () {
        console.log("TODO!!!!")
    });
});